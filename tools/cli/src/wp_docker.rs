/// Builds with the latest base images
/// * copies local files to image
/// * ensures relevant plugins are there,
///   some may have been activated online
/// * somehow uses latest database
/// * ensures plugins and database is in sync
pub fn build_prod_images() {}